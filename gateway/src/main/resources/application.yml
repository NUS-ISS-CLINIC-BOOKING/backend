server:
  port: 8088

spring:
  main:
    web-application-type: reactive
  application:
    name: gateway

  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848

    gateway:
      routes:
        - id: clinic_route
          uri: lb://clinic-service
          predicates:
            - Path=/api/clinic/**

        - id: login_route
          uri: lb://login-service
          predicates:
            - Path=/api/auth/**

        - id: patient_medicine_route
          uri: lb://patient-medicine-service
          predicates:
            - Path=/patient_medicine/**

        - id: queue_route
          uri: lb://queue-service
          predicates:
            - Path=/api/queue/**